spring:
  datasource:
    url: jdbc:postgresql://resources-db:5432/resources
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
     ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  servlet.multipart.max-file-size: 10MB
  servlet.multipart.max-request-size: 10MB
my:
  client:
    audio:
     scheme: http
     host: songs-service
     port: 8080
     endpoint: songs

